import React from 'react';

function Footer(){
    return(
        <footer>
            <p>&copy; {new Date().getFullYear()} ChatStream by Andy </p>
        </footer>
    );
}

export default Footer;
